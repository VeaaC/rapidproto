# CMake build tree (also holds corpus/, fetched by tests/fetch_corpus.py)
/build/

# Bytecode from the tests/ python tooling (fetch_corpus.py, bench.py, check_doc_links.py)
__pycache__/

# Benchmark snapshots emitted by tests/bench.py (carry same-machine cyc/B; not portable, kept local)
/bench_snapshots/

# Conan test_package build tree + Conan-written user presets (packaging dev loop)
/test_package/build/
/CMakeUserPresets.json
