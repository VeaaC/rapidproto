// Generated by rapidprotoc 0.2.4 (debug dumper). DO NOT EDIT.
#pragma once

#include <cstddef>
#include <cstdint>
#include <ostream>
#include <sstream>
#include <string>
#include <type_traits>

#include "wire_all.rp.hpp"  // IWYU pragma: export
#include "rapidproto/debug_runtime.hpp"

namespace rapidproto::debug {

}  // namespace rapidproto::debug

namespace wire {

inline void rp_debug_write(const ::wire::AllWire::G& m, ::rapidproto::debug::Writer& w);
inline void rp_debug_write(const ::wire::AllWire& m, ::rapidproto::debug::Writer& w);

inline void rp_debug_write(const ::wire::AllWire::G& m, ::rapidproto::debug::Writer& w) {
  (void)m;
  w.group('{', '}', [&] {
    bool rp_first = true;
    if (const auto rp_v = m.a()) {
      w.entry_sep(rp_first); w.key("a");
      w.os() << *rp_v;
    }
    (void)rp_first;
  });
}

inline void rp_debug_write(std::ostream& rp_os, const ::wire::AllWire::G& m, std::size_t rp_width = 120) {
  rp_os << std::boolalpha;
  ::rapidproto::debug::Writer w(rp_os, rp_width);
  rp_debug_write(m, w);
}

inline std::string rp_debug_string(const ::wire::AllWire::G& m, std::size_t rp_width = 120) {
  std::ostringstream rp_ss; rp_debug_write(rp_ss, m, rp_width); return rp_ss.str();
}

inline void rp_debug_write(const ::wire::AllWire& m, ::rapidproto::debug::Writer& w) {
  (void)m;
  w.group('{', '}', [&] {
    bool rp_first = true;
    if (const auto rp_v = m.zz()) {
      w.entry_sep(rp_first); w.key("zz");
      w.os() << *rp_v;
    }
    if (const auto rp_v = m.db()) {
      w.entry_sep(rp_first); w.key("db");
      w.os() << *rp_v;
    }
    if (const auto rp_v = m.fx()) {
      w.entry_sep(rp_first); w.key("fx");
      w.os() << *rp_v;
    }
    if (const auto rp_v = m.s()) {
      w.entry_sep(rp_first); w.key("s");
      w.os() << '"'; ::rapidproto::debug::write_json_escaped(w.os(), *rp_v); w.os() << '"';
    }
    if (const auto rp_v = m.by()) {
      w.entry_sep(rp_first); w.key("by");
      w.os() << '"'; ::rapidproto::debug::write_hex(w.os(), *rp_v); w.os() << '"';
    }
    if (const auto* rp_p = m.nested()) {
      w.entry_sep(rp_first); w.key("nested");
      rp_debug_write(*rp_p, w);
    }
    if (const auto& rp_r = m.packed(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("packed");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto* rp_p = m.g()) {
      w.entry_sep(rp_first); w.key("g");
      rp_debug_write(*rp_p, w);
    }
    m.pick([&](auto rp_tag, const auto& rp_v) {
      using RpTag = std::decay_t<decltype(rp_tag)>;
      if constexpr (std::is_same_v<RpTag, ::wire::AllWire::Pick::oi>) {
        w.entry_sep(rp_first); w.key("oi");
        w.os() << rp_v;
      }
      if constexpr (std::is_same_v<RpTag, ::wire::AllWire::Pick::os>) {
        w.entry_sep(rp_first); w.key("os");
        w.os() << '"'; ::rapidproto::debug::write_json_escaped(w.os(), rp_v); w.os() << '"';
      }
    });
    (void)rp_first;
  });
}

inline void rp_debug_write(std::ostream& rp_os, const ::wire::AllWire& m, std::size_t rp_width = 120) {
  rp_os << std::boolalpha;
  ::rapidproto::debug::Writer w(rp_os, rp_width);
  rp_debug_write(m, w);
}

inline std::string rp_debug_string(const ::wire::AllWire& m, std::size_t rp_width = 120) {
  std::ostringstream rp_ss; rp_debug_write(rp_ss, m, rp_width); return rp_ss.str();
}

}  // namespace wire
