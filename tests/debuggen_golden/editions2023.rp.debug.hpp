// Generated by rapidprotoc 0.2.4 (debug dumper). DO NOT EDIT.
#pragma once

#include <cstddef>
#include <cstdint>
#include <ostream>
#include <sstream>
#include <string>
#include <type_traits>

#include "editions2023.rp.hpp"  // IWYU pragma: export
#include "rapidproto/debug_runtime.hpp"

namespace rapidproto::debug {

}  // namespace rapidproto::debug

namespace ed23 {

inline void rp_debug_write(const ::ed23::M& m, ::rapidproto::debug::Writer& w);

inline void rp_debug_write(const ::ed23::M& m, ::rapidproto::debug::Writer& w) {
  (void)m;
  w.group('{', '}', [&] {
    bool rp_first = true;
    if (const auto rp_v = m.implicit_scalar(); rp_v != decltype(rp_v){}) {
      w.entry_sep(rp_first); w.key("implicit_scalar");
      w.os() << rp_v;
    }
    if (const auto rp_v = m.explicit_scalar()) {
      w.entry_sep(rp_first); w.key("explicit_scalar");
      w.os() << *rp_v;
    }
    if (const auto* rp_p = m.child()) {
      w.entry_sep(rp_first); w.key("child");
      rp_debug_write(*rp_p, w);
    }
    if (const auto& rp_r = m.packed_nums(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("packed_nums");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_r = m.expanded_nums(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("expanded_nums");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto* rp_p = m.delim()) {
      w.entry_sep(rp_first); w.key("delim");
      rp_debug_write(*rp_p, w);
    }
    (void)rp_first;
  });
}

inline void rp_debug_write(std::ostream& rp_os, const ::ed23::M& m, std::size_t rp_width = 120) {
  rp_os << std::boolalpha;
  ::rapidproto::debug::Writer w(rp_os, rp_width);
  rp_debug_write(m, w);
}

inline std::string rp_debug_string(const ::ed23::M& m, std::size_t rp_width = 120) {
  std::ostringstream rp_ss; rp_debug_write(rp_ss, m, rp_width); return rp_ss.str();
}

}  // namespace ed23
