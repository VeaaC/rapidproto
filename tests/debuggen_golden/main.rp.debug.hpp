// Generated by rapidprotoc 0.2.4 (debug dumper). DO NOT EDIT.
#pragma once

#include <cstddef>
#include <cstdint>
#include <ostream>
#include <sstream>
#include <string>
#include <type_traits>

#include "main.rp.hpp"  // IWYU pragma: export
#include "rapidproto/debug_runtime.hpp"
#include "dep.rp.debug.hpp"
#include "forward.rp.debug.hpp"

namespace rapidproto::debug {

inline const char* rp_debug_enum_name(::dep::DepEnum rp_e) {
  switch (static_cast<std::int32_t>(rp_e)) {
    case 0: return "DEP_ZERO";
    case 1: return "DEP_ONE";
  }
  return nullptr;  // unknown (open enum): the caller renders UNKNOWN(<n>)
}

}  // namespace rapidproto::debug

namespace main {

inline void rp_debug_write(const ::main::Main& m, ::rapidproto::debug::Writer& w);

inline void rp_debug_write(const ::main::Main& m, ::rapidproto::debug::Writer& w) {
  (void)m;
  w.group('{', '}', [&] {
    bool rp_first = true;
    if (const auto* rp_p = m.d()) {
      w.entry_sep(rp_first); w.key("d");
      rp_debug_write(*rp_p, w);
    }
    if (const auto* rp_p = m.p()) {
      w.entry_sep(rp_first); w.key("p");
      rp_debug_write(*rp_p, w);
    }
    if (const auto* rp_p = m.f()) {
      w.entry_sep(rp_first); w.key("f");
      rp_debug_write(*rp_p, w);
    }
    if (const auto rp_v = m.e()) {
      w.entry_sep(rp_first); w.key("e");
      { const auto rp_e = *rp_v;
      if (const char* rp_nm = ::rapidproto::debug::rp_debug_enum_name(rp_e)) { w.os() << '"' << rp_nm << '"'; }
      else { w.os() << "\"UNKNOWN(" << static_cast<std::int32_t>(rp_e) << ")\""; } }
    }
    if (const auto& rp_r = m.ds(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("ds");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          rp_debug_write(rp_el, w);
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_mp = m.dm(); !rp_mp.empty()) {
      w.entry_sep(rp_first); w.key("dm");
      w.group('{', '}', [&] {
        bool rp_efirst = true;
        for (const auto& rp_ent : rp_mp) {
          w.entry_sep(rp_efirst);
          w.os() << '"' << rp_ent.key() << "\": ";
          if (const auto* rp_vp = rp_ent.value()) { rp_debug_write(*rp_vp, w); } else { w.os() << "null"; }
          if (w.overflowed()) { break; }
        }
      });
    }
    m.choice([&](auto rp_tag, const auto& rp_v) {
      using RpTag = std::decay_t<decltype(rp_tag)>;
      if constexpr (std::is_same_v<RpTag, ::main::Main::Choice::od>) {
        w.entry_sep(rp_first); w.key("od");
        rp_debug_write(rp_v, w);
      }
      if constexpr (std::is_same_v<RpTag, ::main::Main::Choice::oi>) {
        w.entry_sep(rp_first); w.key("oi");
        w.os() << rp_v;
      }
    });
    (void)rp_first;
  });
}

inline void rp_debug_write(std::ostream& rp_os, const ::main::Main& m, std::size_t rp_width = 120) {
  rp_os << std::boolalpha;
  ::rapidproto::debug::Writer w(rp_os, rp_width);
  rp_debug_write(m, w);
}

inline std::string rp_debug_string(const ::main::Main& m, std::size_t rp_width = 120) {
  std::ostringstream rp_ss; rp_debug_write(rp_ss, m, rp_width); return rp_ss.str();
}

}  // namespace main
