// Generated by rapidprotoc 0.2.4 (debug dumper). DO NOT EDIT.
#pragma once

#include <cstddef>
#include <cstdint>
#include <ostream>
#include <sstream>
#include <string>
#include <type_traits>

#include "proto3.rp.hpp"  // IWYU pragma: export
#include "rapidproto/debug_runtime.hpp"

namespace rapidproto::debug {

inline const char* rp_debug_enum_name(::p3::State rp_e) {
  switch (static_cast<std::int32_t>(rp_e)) {
    case 0: return "UNKNOWN";
    case 1: return "ON";
  }
  return nullptr;  // unknown (open enum): the caller renders UNKNOWN(<n>)
}

}  // namespace rapidproto::debug

namespace p3 {

inline void rp_debug_write(const ::p3::Msg& m, ::rapidproto::debug::Writer& w);

inline void rp_debug_write(const ::p3::Msg& m, ::rapidproto::debug::Writer& w) {
  (void)m;
  w.group('{', '}', [&] {
    bool rp_first = true;
    if (const auto rp_v = m.implicit_i(); rp_v != decltype(rp_v){}) {
      w.entry_sep(rp_first); w.key("implicit_i");
      w.os() << rp_v;
    }
    if (const auto rp_v = m.explicit_i()) {
      w.entry_sep(rp_first); w.key("explicit_i");
      w.os() << *rp_v;
    }
    if (const auto rp_v = m.name(); rp_v != decltype(rp_v){}) {
      w.entry_sep(rp_first); w.key("name");
      w.os() << '"'; ::rapidproto::debug::write_json_escaped(w.os(), rp_v); w.os() << '"';
    }
    if (const auto rp_v = m.state(); rp_v != decltype(rp_v){}) {
      w.entry_sep(rp_first); w.key("state");
      { const auto rp_e = rp_v;
      if (const char* rp_nm = ::rapidproto::debug::rp_debug_enum_name(rp_e)) { w.os() << '"' << rp_nm << '"'; }
      else { w.os() << "\"UNKNOWN(" << static_cast<std::int32_t>(rp_e) << ")\""; } }
    }
    if (const auto* rp_p = m.self()) {
      w.entry_sep(rp_first); w.key("self");
      rp_debug_write(*rp_p, w);
    }
    if (const auto& rp_r = m.nums(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("nums");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_r = m.unpacked(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("unpacked");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_r = m.states(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("states");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          { const auto rp_e = rp_el;
          if (const char* rp_nm = ::rapidproto::debug::rp_debug_enum_name(rp_e)) { w.os() << '"' << rp_nm << '"'; }
          else { w.os() << "\"UNKNOWN(" << static_cast<std::int32_t>(rp_e) << ")\""; } }
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_r = m.reals(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("reals");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_r = m.codes(); !rp_r.empty()) {
      w.entry_sep(rp_first); w.key("codes");
      w.group('[', ']', [&] {
        bool rp_efirst = true;
        for (const auto& rp_el : rp_r) {
          w.entry_sep(rp_efirst);
          w.os() << rp_el;
          if (w.overflowed()) { break; }
        }
      });
    }
    if (const auto& rp_mp = m.counts(); !rp_mp.empty()) {
      w.entry_sep(rp_first); w.key("counts");
      w.group('{', '}', [&] {
        bool rp_efirst = true;
        for (const auto& rp_ent : rp_mp) {
          w.entry_sep(rp_efirst);
          w.os() << '"'; ::rapidproto::debug::write_json_escaped(w.os(), rp_ent.key()); w.os() << "\": ";
          w.os() << rp_ent.value();
          if (w.overflowed()) { break; }
        }
      });
    }
    m.pick([&](auto rp_tag, const auto& rp_v) {
      using RpTag = std::decay_t<decltype(rp_tag)>;
      if constexpr (std::is_same_v<RpTag, ::p3::Msg::Pick::a>) {
        w.entry_sep(rp_first); w.key("a");
        w.os() << rp_v;
      }
      if constexpr (std::is_same_v<RpTag, ::p3::Msg::Pick::b>) {
        w.entry_sep(rp_first); w.key("b");
        w.os() << '"'; ::rapidproto::debug::write_json_escaped(w.os(), rp_v); w.os() << '"';
      }
    });
    (void)rp_first;
  });
}

inline void rp_debug_write(std::ostream& rp_os, const ::p3::Msg& m, std::size_t rp_width = 120) {
  rp_os << std::boolalpha;
  ::rapidproto::debug::Writer w(rp_os, rp_width);
  rp_debug_write(m, w);
}

inline std::string rp_debug_string(const ::p3::Msg& m, std::size_t rp_width = 120) {
  std::ostringstream rp_ss; rp_debug_write(rp_ss, m, rp_width); return rp_ss.str();
}

}  // namespace p3
