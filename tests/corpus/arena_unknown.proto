// --unknown-present + a single-bool wrapper field. The wrapper is a normal inlined sub-message, so it
// carries its own has_unknown_fields() in its own mask -- like any message field. Generated with
// --unknown-present and decoded in test_arena_decode.
syntax = "proto3";

package au;

message Flag {  // a single singular bool
  bool value = 1;
}

message Holder {
  Flag flag = 1;  // a single-bool wrapper field -> inlined, accessor returns const Flag*
  int32 n = 2;
}
